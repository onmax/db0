# LibSQL Connector Example

LibSQL database (Turso-compatible SQLite fork).

## Requirements

- Node.js 18+

## Run

```bash
pnpm install
pnpm start
```

## What it shows

- In-memory LibSQL database
- CRUD operations with SQL template literals
- Transaction support
- Compatible with Turso cloud databases
