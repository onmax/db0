# PGlite Connector Example

Embedded PostgreSQL using PGlite (WebAssembly).

## Requirements

- Node.js 18+

## Run

```bash
pnpm install
pnpm start
```

## What it shows

- In-memory PostgreSQL database (no server needed)
- CRUD operations with SQL template literals
- Transaction support
