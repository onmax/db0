# better-sqlite3 Connector Example

SQLite database using better-sqlite3 driver.

## Requirements

- Node.js 18+

## Run

```bash
pnpm install
pnpm start
```

## What it shows

- In-memory SQLite database
- CRUD operations with SQL template literals
- Transaction support
