# MySQL2 Connector Example

MySQL database using mysql2 driver.

## Requirements

- Node.js 18+
- Docker (for MySQL server)

## Run

```bash
docker compose up -d
pnpm install
pnpm start
docker compose down
```

## What it shows

- MySQL database connection
- CRUD operations with SQL template literals
- Transaction support
