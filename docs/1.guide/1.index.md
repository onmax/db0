---
icon: ph:book-open-duotone
---

# Getting Started

> db0 provides an easy way to connect and query sql database providers.

DB0 is a lightweight SQL connector:

::list{type="success"}
- Works with several SQL [connectors](/connectors)
- Can be [integrated](/integrations) with ORMs and embedded into frameworks
- Provides a simple but elegant query API out of the box
- Supports [transactions](/guide/transactions) with automatic commit/rollback
- Automatic [type transformers](/guide/type-transformers) for booleans, dates, and JSON
- Runtime [capability detection](/guide/capabilities) for database features
::

## Quick start

Install [`db0`](https://npmjs.com/package/db0) npm package:

:pm-install{name="db0"}

```ts
import { createDatabase } from "db0";
import sqlite from "db0/connectors/better-sqlite3";

// Initiate database with SQLite connector
const db = createDatabase(sqlite({}));

// Alternative:
// `using` automatically closes the database connection
// once the `db` variable goes out of scope  (for example, the function execution ends)
// await using db = createDatabase(sqlite({}));

// Create users table
await db.sql`CREATE TABLE IF NOT EXISTS users ("id" TEXT PRIMARY KEY, "firstName" TEXT, "lastName" TEXT, "email" TEXT)`;

// Add a new user
const userId = "1001";
await db.sql`INSERT INTO users VALUES (${userId}, 'John', 'Doe', '')`;

// Query for users
const { rows } = await db.sql`SELECT * FROM users WHERE id = ${userId}`;
console.log(rows);

// Using static parameters
const tableName = "users";
const { rows } =
  await db.sql`SELECT * FROM {${tableName}} WHERE id = ${userId}`;
console.log(rows);
```

> [!IMPORTANT] > **Static Parameters** are a way to use string-literals other than places where prepared statements are supported, for eg. table name. **DO NOT USE** static parameters from untrusted source such as request body. **STATIC PARAMETERS ARE NOT SANITIZED**

## Transactions

Execute multiple operations atomically:

```ts
await db.transaction(async (tx) => {
  await tx.sql`INSERT INTO accounts (id, balance) VALUES (1, 1000)`;
  await tx.sql`UPDATE accounts SET balance = balance - 100 WHERE id = 1`;
});
```

:read-more{to="/guide/transactions"}

## Database Capabilities

Check what features your database supports:

```ts
console.log(db.capabilities);
// { supportsJSON: true, supportsBooleans: false, ... }
```

:read-more{to="/guide/capabilities"}

## Database Options

### Eager Connection

Pre-establish connection at creation time:

```ts
const db = createDatabase(connector, { eager: true });
await db.ready(); // Wait for connection
```

### Resource Cleanup

```ts
// Manual cleanup
await db.dispose();

// Automatic with `using` syntax (recommended)
await using db = createDatabase(connector);
// Connection auto-closes when scope exits
```

### Type Transformers

```ts
const db = createDatabase(connector, {
  transformers: { booleans: true, dates: true, json: true }
});
```

:read-more{to="/guide/type-transformers"}

## Next steps

:read-more{to="/connectors"}

:read-more{to="/integrations"}
