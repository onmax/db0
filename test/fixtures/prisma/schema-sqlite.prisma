generator client {
  provider = "prisma-client-js"
  output   = "./client-sqlite"
}

datasource db {
  provider = "sqlite"
  url      = "file:./test.db"
}

model User {
  id    Int     @id @default(autoincrement())
  name  String
  email String?
  age   Int?
}
